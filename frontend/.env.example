# Base URL for the FastAPI backend. In dev, leave unset to use the built-in
# mock at /api (see vite.config.js), which is proxied by Vite's own server.
# Point this at the real backend once it exists, e.g.:
# VITE_API_BASE_URL=http://localhost:8000/api
VITE_API_BASE_URL=/api
